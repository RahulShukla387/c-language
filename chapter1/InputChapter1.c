#include<stdio.h>
int main(){
    printf("emter the value");
    int x;
    // used to store the value given by user
    scanf("%d",&x);
    // print the value
    printf("the value of x is,%d",x );
int y = 9;
printf("\n %d",y);
y = y-30;
printf("\n %d",y);
y = y*3;
printf("\n %d",y); 
char c = 'R';
//Escape sequence character
// if you want to use any symbol like "" or '
printf("\n your character is %c\'rahul",c);
/*you can choose float %f string char %c int %d */
  return 0;
}